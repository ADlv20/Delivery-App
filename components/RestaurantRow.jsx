import { Text, View } from "react-native";
import React from "react";

const RestaurantRow = () => {
    return (
        <View>
            <Text>RestaurantRow</Text>
        </View>
    );
};

export default RestaurantRow;
